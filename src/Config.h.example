#pragma once

// Copy to Config.h and fill in your values.  Config.h is gitignored — never commit it.

// WiFi — both devices must connect to the same AP so ESP-NOW channel is shared.
static constexpr char WIFI_SSID[]     = "your-ssid";
static constexpr char WIFI_PASSWORD[] = "your-password";

// OTA password — required when pushing firmware wirelessly.
static constexpr char OTA_PASSWORD[]  = "slider-ota";
